           
     <%= collection_select :datashow, :unidade_id, @unidades, :id, :nome, :include_blank => ' -- Selecione -- '  %>
                        <%= observe_field 'datashow_unidade_id',
                           :update => "listagem",
                           :url    => { :action => 'lista_tipounidades' },
                           :with   => 'datashow_unidade_id' %>

