           
      <%= collection_select  :entrada, :estoque_id, @estoques, :id, :especifica, :include_blank => ' -- Selecione -- '  %>
            <%= observe_field 'entrada_estoque_id',
               :frequency => 0.1,
               :with => :entrada_estoque_id,
               :url => { :action => 'exibe_obs' } %>