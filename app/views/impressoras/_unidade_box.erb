           
     <%= collection_select :impressora, :unidade_id, @unidades, :id, :nome, :include_blank => ' -- Selecione -- '  %>
                        <%= observe_field 'impressora_unidade_id',
                           :update => "listagem",
                           :url    => { :action => 'lista_tipounidades' },
                           :with   => 'impressora_unidade_id' %>

